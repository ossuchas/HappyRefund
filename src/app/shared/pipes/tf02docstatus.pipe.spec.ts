import { Tf02docstatusPipe } from './tf02docstatus.pipe';

describe('Tf02docstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new Tf02docstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
