version: '3.5'

services: 
  app:
    image: happyrefund
    build:
        context: .
        dockerfile: ./Dockerfile
    command: "yarn run start"
    ports: 
      - "4200:4200"
    expose:
      - "4200"
    volumes:
        - '/home/ubuntu/src/app'
        - '/home/ubuntu/src/app/node_modules'
